# Plantilla de compilador

Copie esta carpeta dentro de:

```text
_extensions/iasi-lua/<nombre>/
```

Después añada el filtro a `_extensions/iasi-lua/_extension.yml` y cambie todas
las apariciones de `engine-name`.
